# CTM 2.0

发布日期：2026-08-14

CTM 2.0 是本平台从探索模型走向真实结局风险评估的一次完整升级。该名称是平台发布版本，不代表当前正式网页后端采用 CTM 网络；正式 PFS 后端为 AC-ICAM V8 五种子 Ridge-Cox 生存模型，CTM 与时间拓扑模型继续作为相互隔离的研究路线保留。

## 主要更新

### 1. 正式 PFS 主线升级为真实结局模型

- 默认后端：`ac_icam_real_outcome_clinical_pfs`。
- 模型发布：`ac_icam_real_outcome_pfs_v8`。
- 数据：246 例 AC-ICAM 结直肠癌患者，71 个 PFS 事件。
- 部署结构：5 个全队列 Ridge-Cox 成员，种子为 `7`、`21`、`42`、`123`、`2026`。
- 临床核心模型：C-index `0.7756`，bootstrap 95% CI `0.7214-0.8251`；36 月 AUC `0.8185`，60 月 AUC `0.8013`。
- 临床 + 实测肿瘤 RNA ICR：C-index `0.7845`；36 月 AUC `0.8294`，60 月 AUC `0.8133`。仅在 ICR 被真实测量且输入完整时启用。
- 网页中的菌群字段不再被错误描述为能够提高正式 PFS 分数；拓扑结果作为独立研究参考展示。

### 2. 新增独立口腔腺瘤内部模型

- 发布标识：`oral_adenoma_internal_v3`，默认关闭，不改变正式 PFS 流程。
- 仅使用口腔拭子或唾液的 381 个菌属特征，不使用粪便、血液或组织数据。
- 内部重复 OOF 结果：灵敏度 `64.71%`（22/34），假阳性率 `5.17%`（3/58），特异度 `94.83%`，ROC AUC `0.9219`。
- 应用端在本地使用经校验的 JSON 数值权重，不反序列化研究用 `joblib` 文件；两类权重均不上传 GitHub。
- 该结果来自 92 例单中心回顾性队列，不是前瞻性或外部临床验证，也不能表述为已经验证的 `<=5 mm` 微小腺瘤性能。

### 3. 前端与输入安全升级

- 新增 TypeScript 类型层、API 契约和可重复构建流程，浏览器加载 `static/generated/app.js`。
- 旧版 JavaScript 前端保存在 `archive/legacy_frontend_vanilla_js_20260814/`，便于本地回滚。
- 对年龄、菌群丰度及其他数值输入增加非有限值、负数、越界和结构错误提示。
- 口腔腺瘤入口使用独立功能开关 `GOA_ENABLE_INTERNAL_ORAL_ADENOMA=1`，普通网页启动保持关闭。

### 4. 研究与复现能力

- 保留 topology_v7 数据生成、结构诊断、边权建模、排序目标、跨队列迁移与性能上限实验代码。
- 历史探索分数 `0.8967` 仅作为潜力上限记录，不作为当前正式结果。
- 正式部署权重在本地使用可审查的版本化 JSON 管理；所有权重、历史检查点、原始队列文件、本地输出、图片及 R 绘图源码均不进入公开源码。
- 药学辅助、输入标准化、证据边界和错误提示继续得到加强。

## 验证

- Python：`312 passed`。
- TypeScript：类型检查和生产构建通过。
- JavaScript：生成文件语法检查通过。
- HTTP：正式 PFS 与可选口腔腺瘤接口冒烟测试通过。
- 口腔模型 JSON 与研究模型在 92 个样本上的判定完全一致，最大概率误差 `1.05e-14`。
- `npm audit`：0 个已知漏洞。

## 使用边界

本版本用于研究、内部评估和演示，不是医疗器械，也不能替代临床诊断、内镜检查、病理检查或专业医疗决策。正式指标均应连同队列、终点和验证方法一起报告。

## GitHub 发布边界

GitHub Release 仅包含可审查源码、配置协议、测试、文档和不含训练权重的复现工具。`output/`、`outputs/`、图片、R 绘图源码、`.pt/.pth` 等训练检查点、joblib/pickle 文件及含拟合系数的部署 JSON 均只保留在本地，未作为源码或 Release 附件上传。
